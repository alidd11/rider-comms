{
  "name": "@rider-comms/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "App.tsx",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "test:client": "node --experimental-strip-types --test tests/*.test.ts"
  },
  "dependencies": {
    "@rider-comms/shared": "*",
    "expo": "~51.0.0",
    "expo-av": "~14.0.0",
    "react": "18.2.0",
    "react-native": "0.74.0",
    "@react-navigation/native": "^6.1.0",
    "@react-navigation/native-stack": "^6.9.0",
    "react-native-screens": "~3.31.0",
    "react-native-safe-area-context": "4.10.0",
    "livekit-client": "^2.5.0",
    "livekit-react-native": "^2.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/react": "~18.2.0"
  },
  "_NOTE": "This app has NOT been npm-installed or run in the build sandbox — the npm registry is blocked by that environment's network policy, and React Native/Expo need real native tooling (Xcode/Android Studio) this sandbox doesn't have either. This is hand-written, unverified scaffold code. Run `npm install` and `npx expo start` on a real dev machine to actually build/run it."
}
